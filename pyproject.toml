[project]
name = "chadx"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.10",
    "orjson>=3.10.15",
    "pydantic>=2.10.6",
]
